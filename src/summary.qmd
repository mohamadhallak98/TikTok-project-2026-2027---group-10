---
title: "TikTok Video View Summary"
format: html
---

```{r}
views <- read.csv("C:\Users\JeroenSwolfs\Documents\TikTok-project-template-2026\data\video_view.csv")
library(dplyr)
library(tidyverse)

x <- nrow(views)
print(x)

str(views)
head(views)
summary(views)
colSums(is.na(views))

overview <- views %>%
  summarize(
    videos = n(),
    creators = n_distinct(creator_id),
    avg_length = mean(video_length_sec),
    avg_impressions = mean(impressions_n),
    avg_watched = mean(watched_n),
    avg_watch_rate = 
    avg_watch_share = 
    avg_watch_seconds = 
  )

print(overview)

mean(views$watch_rate)
mean(views$avg_watch_share)

views$watched_pct <- views$watched_n / views$impressions_n * 100

topvideo <- views[views$impressions_n == max(views$impressions_n), ]
print(topvideo)

hist(views$video_length_sec)

goodvideos <- subset(views, watch_rate > 0.8)
nrow(goodvideos)

# I am (Mohamad) trying to do a push 
```
